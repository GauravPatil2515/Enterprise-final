{
  "scripts": {
    "start": "npm run dev",
    "dev": "concurrently \"uvicorn api.app.main:app --reload --port 8001\" \"npm run dev --prefix frontend\"  --names \"BACKEND,FRONTEND\" --prefix-colors \"blue,green\"",
    "build": "npm run build --prefix frontend",
    "lint": "eslint .",
    "preview": "vite preview"
  },
  "dependencies": {
    "axios": "^1.13.4",
    "clsx": "^2.1.1",
    "framer-motion": "^12.33.0",
    "lucide-react": "^0.563.0",
    "recharts": "^3.7.0",
    "tailwind-merge": "^3.4.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "vercel": "^50.14.0"
  }
}